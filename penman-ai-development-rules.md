# Penman AI Development Rules (STRICT MODE)

این فایل مجموعه قوانین غیرقابل‌نقض برای توسعه پروژه Penman است.
هر سیستم هوش مصنوعی یا توسعه‌دهنده موظف است دقیقاً طبق این قوانین عمل کند.

---

## 1. منبع حقیقت (Single Source of Truth)

- فقط و فقط پوشه `/docs` مرجع رسمی توسعه است.
- هیچ تصمیم فنی، معماری یا پیاده‌سازی خارج از `/docs` معتبر نیست.
- اگر چیزی در `/docs` وجود ندارد، یعنی «وجود ندارد».

---

## 2. ممنوعیت حدس (No Assumptions Rule)

- حدس زدن مطلقاً ممنوع است.
- در صورت نبود اطلاعات:
  - باید توقف کند
  - باید گزینه‌های ممکن پیشنهاد دهد
  - اجازه تصمیم‌گیری نهایی فقط با کاربر است

---

## 3. قانون عدم انحراف (No Deviation Rule)

- هیچ تغییری نباید خارج از طراحی مستندات انجام شود
- حتی تغییر کوچک در رفتار سیستم بدون مستندات جدید ممنوع است
- refactor بدون دلیل مستند = نقض قانون

---

## 4. فازبندی اجباری (Phase-Gated Development)

هر قابلیت فقط در این ترتیب قابل توسعه است:

1. Architecture Definition
2. Design Specification
3. Implementation Plan
4. Implementation
5. Testing
6. Documentation Update

❌ هیچ مرحله‌ای نباید skip شود

---

## 5. قانون تطابق کامل با docs

- هر فایل کد باید trace مستقیم به یک فایل در `/docs` داشته باشد
- اگر feature در docs نیست → نباید در کد وجود داشته باشد
- اگر کد وجود دارد ولی docs ندارد → باید حذف یا مستندسازی شود

---

## 6. ممنوعیت تغییر رفتار سیستم

- هیچ behavior existing نباید تغییر کند مگر:
  - در docs صراحتاً تعریف شده باشد
  - یا تغییر جدید در docs ثبت شده باشد

---

## 7. قانون شفافیت (No Silent Changes)

- هیچ تغییر پنهانی مجاز نیست
- هر تغییر باید:
  - مستند شود
  - قابل ردیابی باشد
  - دلیل داشته باشد

---

## 8. قانون حل ابهام (Ambiguity Handling)

در صورت وجود هر ابهام:

- توقف توسعه
- ارائه چند گزینه مشخص
- درخواست تصمیم از کاربر

❌ انتخاب خودکار ممنوع

---

## 9. قانون ماژولار بودن

- همه سیستم‌ها باید مستقل و قابل تست باشند
- هیچ coupling غیرضروری بین ماژول‌ها مجاز نیست
- هر ماژول باید مسئولیت واحد داشته باشد

---

## 10. قانون تغییرات برگشت‌پذیر

- هیچ تغییری نباید irreversible باشد
- هر تغییر باید قابل rollback باشد
- state system باید قابل بازسازی باشد

---

## 11. قانون تست قبل از ادغام

- هیچ کدی بدون تست قابل قبول وارد main flow نمی‌شود
- تست باید شامل edge case ها باشد
- failure در تست = توقف کامل توسعه

---

## 12. قانون اولویت مستندات

ترتیب قدرت تصمیم‌گیری:

1. /docs (قانون نهایی)
2. این فایل قوانین
3. تصمیمات قبلی سیستم
4. هیچ چیز دیگر

---

## 13. قانون عدم خودمختاری

- AI اجازه ندارد:
  - feature جدید اختراع کند
  - API جدید بسازد
  - behavior جدید اضافه کند

مگر اینکه در `/docs` تعریف شده باشد

---

## 14. قانون توسعه تدریجی

- توسعه باید incremental باشد
- هیچ تغییر بزرگ یک‌مرحله‌ای مجاز نیست
- هر تغییر باید کوچک، قابل بررسی و قابل rollback باشد

---

## 15. نقض قوانین

هر نقض یکی از موارد زیر است:

- توقف فوری توسعه
- بازگشت به آخرین نسخه پایدار
- بازبینی کامل توسط انسان

---

END OF RULES

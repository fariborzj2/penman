# CodeBlock Plugin Usage

- Click the `codeblock` icon in the toolbar.
- If text is selected, it will be wrapped in a code block.
- If it is clicked again inside the code block, the code block will be reverted to a paragraph.

# DirectionPlugin

The DirectionPlugin intelligently applies RTL (Right-to-Left) and LTR (Left-to-Right) text direction to block-level elements in the Penman Editor. It automatically detects the predominant language (Persian, Arabic, English, etc.) and adjusts the direction accordingly, while also providing manual override controls for the user.

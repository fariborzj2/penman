# تحلیل وضعیت پروژه بر اساس شواهد واقعی (Strict Rules)

بر اساس بررسی فایل‌های سیستم (`verify_ui.py`, `src/plugins/`, `docs/`) وضعیت فازهای پروژه بر اساس قوانین دسته‌بندی فازها به شرح زیر تحلیل شده است.

## ۱. فاز 1 تا 6 اصلی پروژه (Core)
- **وضعیت:** **نیمه‌کاره (In Progress / Incomplete)**
- **دلایل فنی (نقص‌ها و گپ‌ها):**
  - در فایل `src/integration.test.js` (که وجود ندارد ولی گزارش شده) و سایر تست‌های یکپارچگی/واحد مانند `src/commands/CommandManager.test.js` و `src/ui/UIManager.test.js` هنوز از Mock (`vi.spyOn`, مسخره کردن state queryها) استفاده می‌شود. استفاده از Mock در تست‌هایی که قرار است فرآیند یکپارچه را تست کنند، نقض قوانین است.
  - این بخش‌ها باید کاملاً مبتنی بر E2E یا تست‌های JSDOM بدون Mock کار کنند.

## ۲. فاز 7: مستندسازی و تثبیت (Documentation & Stabilization)
- **وضعیت:** **نیمه‌کاره (In Progress / Incomplete) / شروع‌نشده**
- **دلایل فنی:**
  - مستندات API عمومی کامل نشده است و فایل‌های مرتبط (توضیح داده شده در PROGRESS.md) هنوز باقی‌مانده‌اند (نقص در مستندسازی طبق مستندات پروژه).

## ۳. افزونه جدول (Table Plugin - فاز 1 تا 5)
- **وضعیت:** **کامل (Completed)**
- **دلایل فنی:**
  - برخلاف فرض قبلی، در فولدر `src/plugins/table/` هیچ تستی از Mock اشیاء `editorMock` یا `vi.fn()` استفاده نمی‌کند (`TableGrid.test.js` بررسی شد).
  - در فایل `verify_ui.py` تست‌های کامل End-to-End شامل `test_editor_table_plugin_e2e` (درج جدول ۲x۲) و `test_editor_table_color_picker_e2e` (تست ColorPicker روی جدول) با مرورگر واقعی و تأیید اثر روی DOM بدون Mock پیاده‌سازی شده و پاس می‌شوند.
  - هیچ TODO در پیاده‌سازی دیده نشد و رفتار کاملاً E2E است.

## ۴. افزونه تصویر (Image Plugin - فاز 1 تا 5)
- **وضعیت:** **نیمه‌کاره (In Progress / Incomplete)**
- **دلایل فنی (نقص‌ها و گپ‌ها):**
  - **نقض امنیتی (Trust Immutability Rule):** در فایل `src/plugins/ImagePlugin/index.js` از دستور `editor.image.insertUntrustedURL` در مواردی استفاده می‌شود که تصویر باید با مجوز `TrustLevel.TRUSTED` درج شود (نقض مستقیم `19-image-plugin-spec.md` بخش 8).
  - **UI نمایشی (شبیه‌سازی Progress):** در تابع `processQueue` در فایل `index.js` برای پیشرفت آپلود از یک logic مصنوعی یا UI شبیه‌سازی‌شده (Progress Simulation) بدون اتکای کامل و بدون نقص به API آپلود استفاده شده است.
  - تست‌های `verify_ui.py` با وجود استفاده از `test-e2e.mjs` (که `server.js` واقعی را در کنار Vite اسپاون می‌کند) آپلود را چک می‌کنند اما کد اصلی افزونه در وضعیت نقض معماری قرار دارد.

## ۵. افزونه جستجو و جایگزینی (Find and Replace Plugin)
- **وضعیت:** **نیمه‌کاره (In Progress / Incomplete)**
- **دلایل فنی:**
  - در فایل `src/plugins/FindReplacePlugin.test.js` از شبیه‌سازی انتخاب DOM استفاده شده است (`window.getSelection = () => ({...})`). هرچند این فایل فقط تست واحد/یکپارچگی در jsdom است، اما طبق قوانین Strict Mode، استفاده از Mock و شبیه‌سازی صریح API بومی برای فرار از تست واقعی غیرمجاز است. این رفتار باید اصلاح شود تا یا در تست E2E واقعی بررسی شود و یا با انتخاب واقعی JSDOM تطبیق یابد.

---

## برنامه مرحله‌به‌مرحله برای تکمیل فازهای نیمه‌کاره (اصلاحات)

طبق قانون اولویت اجرای اصلاحات، هیچ توسعه جدیدی تا رفع این نقص‌ها مجاز نیست:

### مرحله ۱: اصلاح معماری و امنیت Image Plugin
1. **رفع نقض امنیتی:** جایگزینی `editor.image.insertUntrustedURL` با `editor.image.insertFromURL` برای منابع آپلودشده معتبر تا سطح دسترسی `TrustLevel.TRUSTED` رعایت شود.
2. **پاکسازی UI نمایشی:** اصلاح بخش `processQueue` تا از شبیه‌سازی ظاهری پرهیز شده و آپلود منحصراً به تابع `uploadFn` واقعی تکیه کند.

### مرحله ۲: پاکسازی کامل تست‌ها از Mock و شبیه‌سازی (FindReplace و لایه‌های پایه)
1. **FindReplace Plugin:** حذف شبیه‌سازی `window.getSelection = ...` در فایل `FindReplacePlugin.test.js` و اتکا به انتخاب واقعی روی المان‌های DOM (در صورت امکان در JSDOM) یا حذف این تست از اینجا و سپردن آن صرفاً به سناریوی موجود در `verify_ui.py`.
2. **CommandManager و UIManager:** حذف هرگونه `vi.spyOn` یا شبیه‌سازی کوئری DOM در تست‌های واحد مربوطه و جایگزینی آن‌ها با بررسی اثراث واقعی جانبی (Side Effects) روی `innerHTML` یا کلاس‌های المان‌ها.

### مرحله ۳: مستندسازی و نهایی‌سازی (فاز ۷)
1. تکمیل مستندات باقی‌مانده و آپدیت تمام قوانین با تغییرات نهایی اعمال شده در مراحل بالا.
